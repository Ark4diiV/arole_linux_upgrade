name: Linter
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-20.04
    steps:
      - run: sudo apt-get update && sudo apt-get install python python3-pip -y && pip3 install ansible ansible-lint yamllint molecule
      - run: molecule test
