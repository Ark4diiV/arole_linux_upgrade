name: Linter
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: sudo apt-get update && sudo apt-get install python pyton3-pip -y && pip3 install ansible ansible-lint yamllint molecule
      - run: molecule test
